name: Docker

on:
  push:
    branches: [master]
    tags: ["v*"]

env:
  IMAGE: ghcr.io/${{ github.repository_owner }}/dnsserver

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
        with:
          path: DnsServer

      - uses: actions/checkout@v4
        with:
          repository: TechnitiumSoftware/TechnitiumLibrary
          path: TechnitiumLibrary

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "9.0"

      - run: dotnet build TechnitiumLibrary/TechnitiumLibrary.sln -c Release
      - run: dotnet publish DnsServer/DnsServerApp/DnsServerApp.csproj -c Release -o DnsServer/DnsServerApp/bin/Release/publish

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/metadata-action@v5
        id: meta
        with:
          images: ${{ env.IMAGE }}
          tags: |
            type=ref,event=branch
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}

      - uses: docker/build-push-action@v6
        with:
          context: DnsServer
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
